// Package version provides version information for OpenEndpoint
package version

// Version is the current version of OpenEndpoint
// Can be set via ldflags at build time
var Version = "v1.0.0"

// BuildTime is the build timestamp
// Can be set via ldflags at build time
var BuildTime = "dev"
